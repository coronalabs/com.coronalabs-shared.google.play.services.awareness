Sources for the plugin `shared.google.play.services.awareness`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.google.play.services.awareness" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
